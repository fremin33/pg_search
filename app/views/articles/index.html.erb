<%= form_tag root_path, :method => 'Get' do %>
   <%= text_field_tag :search, params[:search] %>
   <%= submit_tag 'Search' %>
<% end %>

<ul>
   <% @articles.each do |article| %>
     <li><%= article.title  %></li>
   <% end %>
</ul>
